version: "3.9"
#services:
#  fail2ban:
#    build:
#      context: ./build/fail2ban 
#    cap_add:
#      - NET_ADMIN
#      - NET_RAW
#    network_mode: host
#    command: '-f -x -v'
#    volumes:
#      - ./data/configs/fail2ban/jail.d:/etc/fail2ban/jail.d
#      - rsyslog_logs:/mnt/logs:ro
#    depends_on:
#      rsyslog:
#       condition: service_started
#    restart: unless-stopped
