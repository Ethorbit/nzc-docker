[sshd]
enabled  = true
filter   = alpine-sshd
port     = ssh
logpath  = /logs/sshd.log
maxretry = 10

[sshd-ddos]
enabled  = true
filter   = alpine-sshd-ddos
port     = ssh
logpath  = /logs/sshd.log
maxretry = 10
