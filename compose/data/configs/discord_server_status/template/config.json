{
    "Token": "${DISCORD_SERVER_STATUS_BOT_TOKEN}",
    "RoomIDList":["${DISCORD_SERVER_STATUS_CHANNEL}"],
    "RolesToNotify": [],
    "GameStatus": "with /server-status",
    "PollingInterval": ${DISCORD_SERVER_STATUS_POLLING_RATE},
    "Servers": [
        {
            "Name": "Gmod :${GMOD_ALPHA_1_PORT}",
            "Address": "localhost",
            "Port": ${GMOD_ALPHA_1_PORT}
        },
        {
            "Name": "Web Server",
            "Address": "localhost",
            "Port": 80
        },
        {
            "Name": "Database",
            "Address": "localhost",
            "Port": ${MYSQL_PORT}
        },
        {
            "Name": "File Sharing",
            "Address": "localhost",
            "Port": ${SSH_PORT}
        }
    ]
}
