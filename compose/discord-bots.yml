version: "3.9"
services:
  discord_permissions:
    build:
      context: ./build/permissions
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - discord_sticky_bot:/mnt/discord_sticky_bot
    command: >
      /bin/sh -c 'chown -R ${stickybot_u}:${discord_g} /mnt/discord_sticky_bot/
      && chmod 2770 -R /mnt/discord_sticky_bot/
      && setfacl -d -m u::rwx,g::rwx,o::- "/mnt/discord_sticky_bot"'
    cap_drop:
      - ALL 
    cap_add:
      - CAP_DAC_OVERRIDE
      - CAP_CHOWN
      - CAP_FOWNER
      - CAP_FSETID
    network_mode: none
    restart: on-failure
  discord_sticky-bot:
    build:
      context: ./build/discord_sticky-message-bot
      args:
        PUID: ${stickybot_u}
        PGID: ${discord_g}
    environment:
      - STICKY_COOLDOWN=600000
      - BOT_TOKEN=${DISCORD_STICKY_BOT_TOKEN}
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ${MOUNT_PROC_CPUINFO}:/proc/cpuinfo:rw
      - ${MOUNT_PROC_MEMINFO}:/proc/meminfo:rw
      - ${MOUNT_PROC_SWAPS}:/proc/swaps:rw
      - ${MOUNT_PROC_STAT}:/proc/stat:rw
      - ${MOUNT_PROC_DISKSTATS}:/proc/diskstats:rw
      - ${MOUNT_PROC_UPTIME}:/proc/uptime:rw
      - ${MOUNT_PROC_SLABINFO}:/proc/slabinfo:rw
      - ${MOUNT_SYS_DEVICES_SYSTEM_CPU}:/sys/devices/system/cpu:rw
      - ./data/configs/users/passwd:/etc/passwd:ro
      - ./data/configs/users/group:/etc/group:ro
      - ./data/configs/users/shadow:/etc/shadow:ro
      - ./data/configs/users/gshadow:/etc/gshadow:ro
      - discord_sticky_bot:/botdb
    cpuset: ${DISCORD_CPU}
    cpu_shares: 256
    #cpu_percent: 10
    deploy:
      resources:
        limits:
          memory: ${DISCORD_STICKY_BOT_RAM:-90M}
        reservations:
          memory: 30M
    blkio_config:
      weight: 50
      device_write_bps:
        - path: ${DISK}
          rate: '10m'
      device_read_bps:
        - path: ${DISK}
          rate: '20m'
      device_read_iops:
        - path: ${DISK}
          rate: 120
      device_write_iops:
        - path: ${DISK}
          rate: 100
    depends_on:
      discord_permissions:
        condition: service_completed_successfully
      discord_rsyslog:
        condition: service_healthy
    logging:
      driver: syslog
      options:
        mode: non-blocking
        syslog-address: "tcp://localhost:${DISCORD_STICKY_BOT_RSYSLOG_PORT}"
        tag: "discord_sticky_bot"
    restart: always
    networks:
      - discord

  discord_rsyslog:
    command: '-n'
    tty: true
    stdin_open: true
    healthcheck:
      test: "/healthcheck.sh"
      start_period: 5s
      interval: 5s
      timeout: 10s
      retries: 3
    restart: always
    build:
      context: ./build/rsyslog
      args:
        - UID=${rsyslog_u}
        - GID=${discord_g}
        - PORT=${DISCORD_STICKY_BOT_RSYSLOG_PORT}
    depends_on:
      rsyslog_permissions:
        condition: service_completed_successfully
    hostname: rsyslog_discord_sticky_bot
    ports:
      - 127.0.0.1:${DISCORD_STICKY_BOT_RSYSLOG_PORT}:${DISCORD_STICKY_BOT_RSYSLOG_PORT}/tcp
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ./data/configs/rsyslog/rsyslog.conf:/etc/rsyslog.conf
      - ./data/configs/logrotate/rsyslog.conf:/etc/logrotate.d/rsyslog
      - discord_sticky_bot_logs:/logs
      - ${MOUNT_PROC_CPUINFO}:/proc/cpuinfo:rw
      - ${MOUNT_PROC_MEMINFO}:/proc/meminfo:rw
      - ${MOUNT_PROC_SWAPS}:/proc/swaps:rw
      - ${MOUNT_PROC_STAT}:/proc/stat:rw
      - ${MOUNT_PROC_DISKSTATS}:/proc/diskstats:rw
      - ${MOUNT_PROC_UPTIME}:/proc/uptime:rw
      - ${MOUNT_PROC_SLABINFO}:/proc/slabinfo:rw
      - ${MOUNT_SYS_DEVICES_SYSTEM_CPU}:/sys/devices/system/cpu:rw
    cpuset: ${DISCORD_CPU}

networks:    
  discord:
    name: discord-bots
volumes:
  discord_sticky_bot_logs:
  discord_sticky_bot: 
